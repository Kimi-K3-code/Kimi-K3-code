# 🛡️ EXTERNAL AGENT HANDOVER & ARCHITECTURAL MANDATE (v3.0)
## Master Blueprint v3.0, 15-Minute SLA & Autonomous Ingestion Engine Consultation
> **وثيقة التكليف والتسليم الهندسي الرسمي الموجهة للوكيل الخارجي (Claude Opus 5 / Sonnet / Codex)**  
> **القيادة الهندسية العليا:** الباشمهندس زيزو (Product Visionary) & المهندس بولا (Lead Architect)  
> **الجهة المنفذة محلياً:** الأيجنت فلاش (Flash @ Antigravity IDE)  
> **المستودع الرسمي:** `https://github.com/Claude-Opus-5-code/Claude-Opus-5-code.git`  
> **الفرع المعتمد حصراً:** `genspark_ai_developer` (Latest Commit: `c521c19`)  
> **مجلد عمل الوكيل المعزول:** `genspark/` (في الروت بجوار `__gateway-service/` و `🟢_syntx_ai/`)  
> **الدستور الحاكم:** دستور بولا الهندسي v1.2 & عقد البوابة ADR-0008  

---

### 👑 رسالة ترحيبية وتوجيه أولي للوكيل الخارجي (Greeting & Core Mandate):
> **عزيزي الوكيل الخارجي / الزميل المهندس،**  
> أهلاً بك في جلسة عمل جديدة ونظيفة تماماً (Fresh Blank-Slate Session).  
> لقد أنجزنا بنجاح أول مزود إنتاجي متكامل (`syntx`) داخل خدمة البوابة الرسمية (`__gateway-service`)، ونحن لا نبدأ من الصفر، بل نسلمك منظومة إنتاجية مكتملة ومختبرة بنسبة 100% خضراء (171/171 tests green)، ونطلب استشارتك ومراجعتك المعمارية الدقيقة لنقل المنظومة إلى الجيل الثالث والرابع.

---

## 🔒 1. القاعدة الدستورية الصارمة: مجلد العمل المعزول `genspark/` (Zero-Touch Policy)

> ⛔ **توجيه قطعي غير قابل للتفاوض من الباشمهندس زيزو والمهندس بولا (دستور بولا - القانون 10):**
> 1. **الفرع المعتمد حصراً:** كافة أعمالك وتحديثاتك تكون على فرع `genspark_ai_developer`.
> 2. **مجلد العمل المعزول `genspark/`:**  
>    - قم بإنشاء مجلد خاص بك في الروت باسم **`genspark/`** (أول كلمة من اسم الفرع).
>    - هذا المجلد هو ساحتكم المستقلة (Sandbox Workspace): اكتب فيه خططك، مقترحاتك، سجلات الذاكرة، السياق، واستئناف العمل كما تشاء.
> 3. **حظر التعديل المباشر داخل الجتواي (Zero Direct Edits in `__gateway-service/`):**  
>    - **ممنوع منعاً باتاً تعديل أي كود أو إضافة أي ملف مباشرة داخل `__gateway-service/`**.
>    - كل شيء تقترحه أو تختبره يتم داخل مجلدك `genspark/`، حتى يراجعه الباشمهندس زيزو والمهندس بولا ويعتمدان دمجه جراحياً.

---

## 📦 2. حزمة التسليم الهندسية المتاحة لديك في المستودع (The Complete Handover Package)

كل شيء تحتاجه موجود ومرفوع بالكامل على الفرع `genspark_ai_developer`:

| المسار في المستودع | المحتوى والوظيفة الهندسية |
|:---|:---|
| **`__gateway-service/`** | **محرك البوابة الإنتاجية بالكامل**، العقود (`contracts.py`)، وسجل المزودين (`provider_registry.py`). |
| **`__gateway-service/providers/syntx/`** | **المزود المرجعي القياسي المكتمل (Canonical Reference)**: مبني بنمط الـ 4 ملفات النظيفة (`__init__.py`, `definition.py`, `_core.py`, `adapter.py`) + `models_metadata.json` + خزان 37 حساباً نشطاً. |
| **`__gateway-service/docs/`** | **ثلاثية الماستر بلوبرنت**: <br>• `UNIVERSAL_PROVIDER_SPEC_AND_BLUEPRINT.md` (v1.0 مع بولا)<br>• `UNIVERSAL_PROVIDER_SPEC_AND_BLUEPRINT_V2.md` (v2.0 التغذية الاستباقية، جلسات البريد، تفريغ الصوت، مثلث الاختبارات)<br>• `UNIVERSAL_PROVIDER_SPEC_AND_BLUEPRINT_V3.md` (v3.0 معيار الـ 15 دقيقة والتوليد الآلي). |
| **`__gateway-service/tools/har_to_provider.py`** | **أداة التوليد الآلي الصاروخي** التي تفلتر ملفات الـ HAR وتستخرج الموديلات وتولد المزود الإنتاجي في أقل من 30 ثانية. |
| **`🟢_syntx_ai/Root/`** | **النواة السداسية للذاكرة التشغيلية**: `VOICE_LOG.md` (تفريغ 103 فويسات لزيزو وبولا)، `PROGRESS.md`، `tasks.md`، `memory.md`، `ai_state.json`. |
| **`🟢_syntx_ai/har/`** | **سجلات الشبكة الحية الخام (Ground Truth HAR)** المسحوبة حياً من المتصفح (`syntx.ai....1....har` و `syntx.ai....2....har`). |

---

## 🏆 3. ما تم إنجازه واختباره فعلياً (The Proven Production Baseline)

1. **المزود المرجعي Syntx AI مكتمل 100%:**
   - يدعم 28 موديلاً مجانياً مع كافة القدرات (التفكير العميق `thinking: True`، البحث المعمق `deep_research: True`، وتفعيل الأدوات تلقائياً).
   - اجتاز درع الحماية الفوري للموديلات غير البصرية (Non-Vision Guard) في أقل من 3 ميلي ثانية.
   - يدعم تفريغ الصوت المباشر بنجاح مثبت حياً (`transcribe_audio` عبر Whisper-1).
2. **منظومة الحسابات والخزان الذاتي:**
   - 37 حساباً نشطاً في `accounts_syntx.json` محمية بقفل نظامي مشترك `FileLock`.
   - تسجيل تلقائي ناجح ومستقل في 8 ثوانٍ عبر `TempMailClubClient` مع قراءة OTP من Livewire Morphing والتنظيف الذري `delete_email()` لتفادي كمين `MAX 5 limit`.
3. **سلامة الاختبارات الهندسية (100% Green Test Suite):**
   - اجتياز **171 من أصل 171 اختباراً هرمتيكياً معزولاً** في `pytest tests/` بزمن قياسي (1.39 ثانية).
   - اختبارات الضغط الميداني `test_live_gateway.py` و `test_stress_gateway.py` اجتازت بنجاح تام.

---

## 🎯 4. التكليف الهندسي المطلوب منك في هذه الجلسة (Your Exact Mission)

بناءً على توجيهات البروفيسور زيزو (فويس 97 إلى 103):

### 📁 الخطوة 0 (إلزامية قبل أي رد أو كود):
- أنشئ مجلدك المعزول **`genspark/`** في الروت.
- ضع فيه ملف خطتك المقترحة `genspark/PLAN.md` وملف الذاكرة الخاص بك `genspark/memory.md`.

### 🔍 المهمة الأولى: مراجعة الأقران لـ Blueprint v3.0 ومعيار الـ 15 دقيقة (Peer-Review)
- راجع وثيقة `UNIVERSAL_PROVIDER_SPEC_AND_BLUEPRINT_V3.md`.
- هل جدول الـ 15 دقيقة الموضح في القسم 1 واقعي ومحكم؟
- هل المعمارية تغلق كافة الثغرات وتمنع أي ارتباك للمطور أو الأيجنت عند إدخال مزود جديد (مثل NoteGPT أو UseAI)؟

### 🛠️ المهمة الثانية: فحص وتطوير أداة التوليد الآلي `tools/har_to_provider.py`
- شدد الباشمهندس زيزو في فويس 99 و 100 على نقطة جوهرية:  
  **"أهم حاجة ميكونش السكريبت يموت، أو يكون فيه حاجات ميتة ملهاش لازمة! لازم يجيب التنقلات ديناميكياً بالريكوستات بنفس الجلسة، وكل حركة عملناها ونجحت في المعمل تتحط في السكريبت!"**
- فحص كود `__gateway-service/tools/har_to_provider.py`:
  1. تدقيق جلسة `curl_cffi` بـ Chrome impersonation لحفظ الكوكيز والـ CSRF.
  2. تدقيق ربط ريكويست إرسال الـ OTP بريكويست تأكيد الـ OTP المستخرجين من الـ HAR بنفس تسلسل البيانات.
  3. تدقيق عميل البريد المؤقت مع التنظيف الذري `delete_email()` وقراءة الـ OTP من Livewire DOM Morphing.
  4. اقترح في مجلدك `genspark/` أي إضافات تزيد من ذكاء الأداة.

### 🚀 المهمة الثالثة: رسم معالم الماستر القادم Blueprint v4.0 (Autonomous Self-Healing)
- اقترح الأفكار المعمارية للجيل القادم v4.0:
  - استيعاب أي تغير في الـ Schema تلقائياً دون الحاجة لتعديل الكود يدوياً.
  - الرصد الآلي لانقطاع الجلسات أو تبدل كوكيز الحماية واستعادة الاتصال ذاتياً.

---

## ⚖️ 5. الضوابط الدستورية الصارمة (Bolla Constitution v1.2 — Non-Negotiable)

1. **قانون الإسناد المرجعي الصريح بالسطور (Law 8 Ironclad):**  
   - محظور تماماً التخمين أو استخدام عبارات مثل "أعتقد" أو "غالباً".
   - أي نقد أو اقتراح يجب أن يستند إلى دليل سطري صريح من الكود القائم في المستودع أو ملفات الـ HAR.
2. **قانون عدم التراجع أو كسر القائم (Zero Regressions):**  
   - الـ 171 اختباراً الهرمتيكية القائمة خط أحمر؛ لا يجوز اقتراح أي تعديل يكسر اختباراً واحداً منها.
3. **قانون الـ 4 ملفات النظيفة (Zero Sprawl Standard):**  
   - يمنع منعاً باتاً تشتيت المزود في ملفات كثيرة؛ كل مزود ينحصر حصراً في: `__init__.py`, `definition.py`, `_core.py`, `adapter.py`.
4. **أدوار القيادة:**  
   - المراجعة والقرارات المعمارية للباشمهندس زيزو وبولا؛ والتنفيذ البرمجي والمحلي يتم عبر فلاش في Antigravity IDE.

---

### 📋 كيف تبدأ ردك؟
1. أكد إنشاء مجلدك المعزول `genspark/` واعتماد فرع `genspark_ai_developer`.
2. قدم تحليلك ونقدك المعماري لـ Blueprint v3.0 وأداة `har_to_provider.py` بنقاط محددة ومباشرة دون حشو.
3. ضع مسوداتك ومقترحاتك داخل `genspark/` وانتظر توجيهات زيزو وبولا.
